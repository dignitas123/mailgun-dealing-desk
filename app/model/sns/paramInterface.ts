export const message = {
  provider: process.env.EMAILPROVIDER,
  timestamp: "timestamp", // a timestamp (refers to the timestamp from expected result, see 'test.json')
  type: "event", // pattern here of which type it was
}
